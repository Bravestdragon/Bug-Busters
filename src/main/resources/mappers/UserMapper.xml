<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.onlineeducationplatform.mapper.UserMapper">

    <insert id="insertUser" parameterType="com.example.onlineeducationplatform.model.User" 
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, email, role)
        VALUES (#{username}, #{password}, #{email}, #{role})
    </insert>

    <select id="selectAllUsers" resultType="com.example.onlineeducationplatform.model.User">
        SELECT * FROM users ORDER BY id
    </select>

    <select id="selectUserById" parameterType="int" 
            resultType="com.example.onlineeducationplatform.model.User">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <select id="selectUserByUsername" parameterType="string" 
            resultType="com.example.onlineeducationplatform.model.User">
        SELECT * FROM users WHERE username = #{username}
    </select>

    <update id="updateUser" parameterType="com.example.onlineeducationplatform.model.User">
        UPDATE users 
        SET username = #{username}, password = #{password}, email = #{email}, role = #{role}
        WHERE id = #{id}
    </update>

    <delete id="deleteUser" parameterType="int">
        DELETE FROM users WHERE id = #{id}
    </delete>

</mapper>